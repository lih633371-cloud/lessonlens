# LessonLens · 课件质量诊断

一个课件/教案质量评估工具原型。粘贴教案文本，从教学目标对齐度、认知负荷分布、互动设计密度、视觉层次清晰度、风格一致性五个维度打分，生成雷达图并给出结构化的具体改进建议。

[在线体验 →](https://lih633371-cloud.github.io/lessonlens/)

> 📌 当前部署的是**产品原型演示版**：下拉选择预设课件样本查看完整诊断结果。完整版通过 Claude API 支持任意课件文本的实时分析，架构见下方说明。

---

## 这是什么，不是什么

**这不是一个语法检查器。** 工具不检查错别字或病句，评估的是课件的教学设计质量——结构是否合理、节奏是否可控、互动是否充分。

**这是把"课件好不好"这种主观判断转化为可复查的评分体系。** 教研评课时最常见的问题是评价标准因人而异，同一份课件不同评委给出的意见可能完全相反，很难沉淀成可复用的改进标准。这个工具把评估拆解成五个具体维度，每个维度有明确的评分依据，并且改进建议强制包含"问题描述+改进方案+预期效果"三段式结构，避免"整体再优化一下"这种没有行动指向的空泛反馈。

## 五个诊断维度

- **教学目标对齐度（30分）**：内容是否紧密围绕教学目标展开
- **认知负荷分布（25分）**：信息密度是否合理，是否存在短时间信息过载
- **互动设计密度（20分）**：师生/生生互动环节是否充分
- **视觉层次清晰度（15分）**：信息层级是否分明，重点是否突出
- **风格一致性（10分）**：语言风格、术语使用是否统一

## 为什么是演示模式

完整版的核心能力是接入 Claude API，粘贴任意课件文本后实时分析并生成诊断结果。但直接在纯前端页面里调用 API 会暴露密钥，生产环境的正确做法是加一层后端代理（比如 Cloudflare Worker）把密钥托管在服务端。

考虑到这是一个作品集展示项目而非长期运营的产品，我选择用**预生成的示例数据**替代实时调用：交互逻辑、SVG 雷达图渲染算法、动画效果与生产版本完全一致，只是数据来源从"实时 API"换成了"内置 JSON"。演示版特意准备了一份高分示例和一份低分示例，能更直观地体现雷达图在不同课件质量下的差异化呈现效果。

如果要恢复实时生成能力，只需要：
1. 搭建一个轻量后端代理（Worker / Serverless Function），密钥存在服务端环境变量
2. 把前端的 `DEMO_DATA` 数组替换回 `fetch()` 调用
3. `renderRadarChart(data)` 等渲染函数无需改动，输入数据结构一致即可复用

## 特点

- **纯前端，零依赖**：不使用任何框架，一个 HTML 文件即可运行
- **纯 SVG 雷达图**：带缓动展开动画，不依赖 Chart.js 等图表库
- **结构化改进建议**：每条建议强制包含问题、方案、预期效果三个字段，不允许空泛表述

## 本地运行

```
python3 -m http.server 8000
# 访问 http://localhost:8000
```

## 部署到 GitHub Pages

1. push 到 GitHub 仓库
2. Settings → Pages → Source 选择 `main` 分支 `/ (root)`
3. 通过 `https://<用户名>.github.io/<仓库名>/` 访问

## 设计依据

五维评分框架的设计融合了两方面经验：教学目标对齐度、互动设计密度的判断标准来自实际的教学评课经验；认知负荷分布、视觉层次清晰度的评估逻辑，则借鉴了影视内容创作中"信息节奏控制"和"视觉引导"的判断方法——课件设计和视频剪辑在"如何在有限时间内让观众/学生消化信息而不过载"这一点上，逻辑是相通的。

## 关于我

传媒内容创作与教学，5年经验。中国传媒大学戏剧影视导演专业本科，做过品牌商业影视脚本，也长期做 AI 内容工具的实践与教学。

## License

MIT
